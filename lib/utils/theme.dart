import 'package:flutter/material.dart';

class MyTheme {
  static const Color primaryColor = Color(0xff18b5e7); // ana rengimiz
  static const Color primaryColorDark = Color(0xff184ee7);
  static const Color highlightColor = Color(0xff18e7b1);
  static const Color highlightColorDark = Color(0xff149c7c);
  static const Color black = Colors.black;
  static const Color white = Colors.white;
  static const Color grey = Color(0Xffe7ebee);
  static const Color darkGrey = Color(0Xffb4c1c4);

  static const BoxShadow kBoxShadow26 = BoxShadow(
    color: Colors.black26,
    offset: Offset(0.0, 2.0),
    blurRadius: 6.0,
  );

  static const BoxShadow kBoxShadow45 = BoxShadow(
    color: Colors.black45,
    offset: Offset(0.0, 2.0),
    blurRadius: 6.0,
  );
}
